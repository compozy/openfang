# PRD Tasks 1–15: UI Integration Analysis

> Generated: 2026-03-27
> Source: `/tasks/prd-compozy/_task_N.md` + `_task_N_review.md`

---

## Summary

Tasks 1–15 cover infrastructure, persistence, provider integration, and workflow definition pipelines. The majority are pure backend/infra with no direct UI surface. Tasks 6, 8, 9, 13, 14, and 15 each expose new data or API endpoints that have UI implications.

---

## Task-by-Task Analysis

| Task# | Title | Backend Feature | UI Needed? | UI Description | Status |
|-------|-------|-----------------|------------|----------------|--------|
| 1 | Split Persistence Config For Dual Databases | New `PersistenceConfig` struct with `runtime_db` and `compozy_db` paths; `KernelConfig` updated; config validation; TOML `[persistence]` table added | Maybe | The config status API endpoint (`/api/config` or equivalent) could surface `persistence.runtime_db` and `persistence.compozy_db` paths. No new page needed, but the existing Settings/Config view in the dashboard could show dual-DB paths instead of the old single path. Low priority — developer-facing diagnostic info. | PASS |
| 2 | Dual-Database Bootstrap In Kernel Startup | `DatabaseManager` opening both `runtime.db` and `compozy.db`; `db_health()` method; `KernelError::BootFailed` with named-DB error messages | Maybe | `db_health()` is a new kernel capability. If the dashboard has a health/status panel, it could expose per-database health (`runtime.db healthy`, `compozy.db healthy`) rather than a single "database OK" boolean. No new page; modification to existing health/status component. | PASS |
| 3 | Reusable Migration Runner For Both Databases | `MigrationStep`, `MigrationError`, `DatabaseIdentity` enum; `schema_migration` tracking table in both databases; migration runner in `db_migration.rs` | No | Purely internal infra. The migration history in `schema_migration` tables could eventually power a diagnostics page, but no immediate UI requirement. | PASS |
| 4 | Copy Arky Crates Into OpenFang Workspace | 10 Arky crates (`arky-config`, `arky-protocol`, `arky-provider`, `arky-codex`, `arky-claude-code`, `arky-error`, `arky-hooks`, `arky-session`, `arky-tools`, `arky-mcp`) registered as workspace members | No | Pure workspace plumbing. No user-visible API or data model changes. Enables provider tasks 10–12, which do have UI implications later. | PASS |
| 5 | Shared Definition Contract Types | `ContractNode`, `ContractKind` enum (13 variants: 7 structural + 6 semantic), `ContractSchema`, `normalize()`, `validate()`, `ContractValidationIssue` in `openfang-types::contract` | No | Foundation type layer used by agent and workflow compile pipelines. No direct API surface yet. When agent/workflow definition editors are built in the UI, this contract schema will drive the form field types and validation feedback. | PASS |
| 6 | Initial runtime.db Schema And Stores | `agent_runtime`, `agent_session`, `agent_message`, `schedule_runtime`, `schedule_execution` tables; `AgentRuntimeStore`, `AgentSessionStore`, `AgentMessageStore`, `ScheduleRuntimeStore`, `ScheduleExecutionStore` adapters; `/api/health` updated to use `AgentRuntimeStore` | Yes | **Agent runtime state is now durable and queryable.** The dashboard Agents page can show: persistent agent state (`loaded`, `healthy`, `state`, `mode`, `active_session_id`, `active_dispatches`, `last_active_at`). Schedule pages can show persistent execution history from `schedule_execution`. Existing in-memory-only displays should be upgraded to read from the store. Also enables: per-agent session history list, per-schedule execution log. Affects existing Agents page and Scheduler page. | PASS |
| 7 | Workflow Definition Source-Of-Truth Consistency | `WorkflowDefinitionStore` abstraction; create/update/delete paths atomically sync file + memory; `load_workflows_from_dir` restart-safe; HTTP 500 on file-write failure | No | Backend correctness fix for data consistency. Transparent to the UI. The dashboard will no longer silently lose workflow mutations on restart, but no new UI surface is required. Error messages may improve (500 with reason vs silent corruption). | PASS |
| 8 | Workflow Bootstrap And Readiness Semantics | `bootstrap_workflow_definitions()` synchronous startup method; `WorkflowRegistryReadiness` flag (Bootstrapping → Ready); `WorkflowBootstrapResult` with loaded/skipped/error counts; startup ordering: bootstrap → background agents → HTTP bind | Yes | **Readiness state is now trackable.** Two gaps flagged in review: (1) `GET /api/v1/workflows/{id}/runtime` hardcodes `loaded: true` instead of checking `is_ready()` — the UI currently shows incorrect data for workflows during bootstrap. (2) A dashboard status indicator showing "Workflow registry: ready / bootstrapping" would be useful, but this requires the API to expose the readiness flag. This is a fix needed before any meaningful workflow run UI. Affects the Workflows page (runtime endpoint returns stale `loaded: true`). | PASS (with noted gaps) |
| 9 | Initial compozy.db Workflow Core Schema | `workflow_run`, `workflow_checkpoint`, `workflow_signal` tables; `WorkflowRunStore`, `WorkflowCheckpointStore`, `WorkflowSignalStore` adapters; startup recovery scan (downgrades `running` → `paused`); `WorkflowStoreSet` replaces raw `compozy_db` handle | Yes | **Durable workflow run state is now persisted.** Major UI opportunity: a Workflow Runs list page showing all `workflow_run` rows with status, timestamps, step progress. Checkpoint history viewer per run. Signal delivery UI (manually send a signal to a `waiting_signal` run). Run recovery status (was it downgraded to `paused` on restart?). These are new pages not currently in the dashboard. Endpoints to surface this data are provided by the stores but the API routes need to be wired (see Task 15 for compile endpoints; run management endpoints are a future task). | PASS |
| 10 | Provider Layering For Workspace, Profiles, And Agent Config | Three-tier config merge (workspace < profile < agent); `profiles: BTreeMap<String, ProviderProfileConfig>` in `ArkyConfig`; `resolve_agent_provider()` function; `validate_config()` extended for profiles and agent-level provider config; `ProviderProfileConfigBuilder` | Yes | **Named provider profiles are a new user-facing concept.** The dashboard Agent editor should allow: selecting a profile from a dropdown (profiles defined in workspace config), viewing the merged/resolved provider config for an agent. A new Profiles section in Settings (or a standalone Profiles page) where users can see and manage named profiles (`fast-research`, `safe-doc-writer`, etc.). Currently the dashboard has no concept of provider profiles. | PASS |
| 11 | ProviderBinding Compile Layer For Compozy Agents | New `openfang-provider-binding` crate; `ProviderBinding` struct (driver, provider_id, model, profile, defaults, config); `compile_provider_binding()` pure synchronous function; `CompileError` enum with `ClassifiedError` impl | No | Internal compile artifact. Not exposed directly to users. Used by the agent definition compiler (future task). The `CompileError` types could surface in an agent validation UI once the agent compile endpoint exists (future task). No direct UI change needed now. | PASS |
| 12 | Typed Provider Integration For Codex And Claude Code | `binding_to_codex_config()`, `binding_to_claude_code_config()`, `binding_to_claude_compatible_config()` adapters; `build_provider_config()` dispatch; `CompozyProviderConfig` enum (`Codex`, `ClaudeCode`, `ClaudeCompatible`); `AdapterError` with `ClassifiedError` impl | No | Pure adapter layer. No user-visible change. Enables correct provider instantiation at runtime, but the UI surface for this is the agent editor's provider config fields (future task). `tracing::warn!()` emitted for unsupported wrapper fields — could be surfaced in a run log UI eventually. | PASS |
| 13 | Workflow v2 Definition Types | `WorkflowV2Definition` and `WorkflowV2Step` types in `openfang-types`; `StepKind` enum (8 variants: Agent, Primitive, Workflow, WaitSignal, StartLooper, EmitEvent, Collect, Noop); `FlowMode` enum (Sequential, FanOut, Conditional, Loop); `WorkflowDefaults`, `RuntimeBlock`, `FlowBlock` structs; all types `Serialize + Deserialize` | Yes | **The workflow definition schema is now fully typed.** The dashboard Workflow editor should be updated to: show all 8 step kind options; expose the `flow` block with mode selection and mode-specific required fields (`when` for conditional, `until`+`max_iterations` for loop); show `save_as` symbol binding per step; show `input`/`output` contract fields using `ContractKind` vocabulary. Also the workflow list and detail views should display v2 fields like `version`, `enabled`, `tags`. This is a significant redesign of the Workflow editor page. | PASS |
| 14 | Workflow v2 Compile Pipeline | `validate_workflow_definition()`, `normalize_workflow_definition()`, `compile_normalized_workflow()` pipeline in `workflow_compiler.rs`; `WorkflowIr` struct (serializable, stable); `NormalizedWorkflow` intermediate; `ValidationIssue` with severity/code/path/message; forward/dangling reference detection; runtime executor updated to accept only `WorkflowIr` | Yes | **Validation feedback is now structured and actionable.** The Workflow editor should display inline validation issues as users author definitions (severity badge, code, message, path highlighting). The compile endpoint (Task 15) exposes this — the UI should consume it for live validation. A "Validate" button and "Compile" button in the editor, with results displayed per-step at the correct path. Symbol table view (which `save_as` symbols are in scope at each step). Impacts the Workflow editor page. | PASS |
| 15 | Workflow v2 API Endpoints | `POST /api/v1/workflows/validate`; `POST /api/v1/workflows/compile`; `GET /api/v1/workflows/{id}/compiled` — all returning structured JSON per API-SPEC.md; registered in `server.rs` under `/api/v1/` prefix; request/response types in `types.rs`; standard error envelope | Yes | **Three new API endpoints ready for UI consumption.** The Workflow editor can call `POST /api/v1/workflows/validate` for live feedback as the user edits. `POST /api/v1/workflows/compile` can be called on save, showing the compiled IR summary. `GET /api/v1/workflows/{id}/compiled` supports a "view compiled IR" panel in the detail view. **Note from review:** integration tests are absent from the API crate — this is a code quality gap but does not block the UI from using the endpoints. Also: `get_workflow_compiled` re-runs the pipeline on each request rather than caching, which may be slow for the UI; consider showing a spinner or loading state. | PASS (with noted gaps) |

---

## Grouped UI Work Required

### Existing Pages That Need Updates

| Page | Tasks Driving the Change | What to Update |
|------|--------------------------|----------------|
| Agents page | Task 6 | Show persistent `agent_runtime` state: `loaded`, `healthy`, `state`, `mode`, `active_dispatches`, `last_active_at` (currently in-memory only). Add per-agent session history tab. |
| Scheduler page | Task 6 | Show `schedule_execution` history from persistent store (previously in-memory only). |
| Workflow list + detail | Tasks 7, 8, 13, 15 | v2 fields: `version`, `enabled`, `tags`, `input`, `output`. Runtime status: `loaded` should reflect actual readiness from `WorkflowRegistryReadiness` (currently hardcoded `true`). |
| Health/status panel | Tasks 2, 6 | Per-database health (`runtime.db`, `compozy.db`). Agent runtime state from store. |
| Config/Settings page | Task 1 | Show `persistence.runtime_db` and `persistence.compozy_db` paths. |

### New Pages/Sections Needed

| New Page/Section | Tasks Driving the Need | Description |
|------------------|------------------------|-------------|
| Workflow Runs page | Task 9 | List all `workflow_run` rows: status, workflow_id, started_at, completed_at, current_step. Filter by status. Click to see run detail. |
| Workflow Run detail | Task 9 | Checkpoint history (ordered by `created_at`). Signal delivery panel (list pending signals; button to send signal to `waiting_signal` runs). Run recovery status. |
| Workflow editor (redesign) | Tasks 13, 14, 15 | Full v2 step editor with all 8 step kinds; flow mode picker; `save_as` binding per step; `input`/`output` contract editor; inline validation issues from `POST /api/v1/workflows/validate`; compile button calling `POST /api/v1/workflows/compile`; compiled IR viewer. |
| Provider Profiles section | Task 10 | List named profiles from workspace config (`fast-research`, `safe-doc-writer`, etc.). Profile detail: driver, model, defaults, behavior config. Used as reference when editing agent provider config. |

### Items Blocked or Incomplete (Gaps from Reviews)

| Gap | Task | Impact |
|-----|------|--------|
| `GET /api/v1/workflows/{id}/runtime` hardcodes `loaded: true` | Task 8 | UI shows wrong data for workflows during bootstrap window. Must fix before shipping the Workflow detail page. |
| Integration tests for Task 15 endpoints absent | Task 15 | No automated contract guarantee for the three workflow compile/validate/compiled endpoints. Low risk for UI but means endpoint behavior is only tested manually. |
| `get_workflow_compiled` re-runs pipeline on every request | Task 15 | Performance concern for UI: calling this endpoint per-workflow in a list view will be slow. UI should call sparingly or cache client-side. |
| Task 9 initial migration has status values diverging from spec | Task 9 | `waiting_signal` and `paused` not in initial migration CHECK constraint (added later via migration 0006/0007). No immediate UI impact since later migrations fix it, but the run status vocabulary in the UI must match the current schema, not the spec. |
